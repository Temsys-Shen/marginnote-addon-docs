---
title: UIActivityIndicatorView
description: 加载指示器（菊花）。
---

`UIActivityIndicatorView` 是常见的加载指示器控件。

## 实例成员 (Instance members)

`UIActivityIndicatorView` 的成员用于控制“是否在转/是否隐藏/样式”等。若你需要具体成员名，用站内搜索定位。

## 类成员 (Class members)

当前接口未额外暴露 `UIActivityIndicatorView` 的类成员。

## 提示

- 常用于在插件面板里表示“加载中”。
- 具体属性/方法可用站内搜索成员名快速定位。

