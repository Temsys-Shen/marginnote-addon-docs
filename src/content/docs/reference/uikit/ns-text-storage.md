---
title: NSTextStorage
description: 富文本存储（可变 attributed string 的存储与通知层）。
---

`NSTextStorage` 是富文本存储类型，常出现在 `UITextView` 等文本系统中，用于承载可变富文本并触发相关更新。

## 实例成员 (Instance members)

`NSTextStorage` 在插件环境中通常由 `UITextView.textStorage` 获取并间接使用。若需要定位某个成员名，建议用站内搜索。

## 类成员 (Class members)

当前运行时未额外暴露 `NSTextStorage` 的类成员。

## 提示

- 在插件中通常通过 `UITextView` 等组件间接使用。
- 具体可用成员建议使用站内搜索成员名。

