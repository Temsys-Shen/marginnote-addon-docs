---
title: CATransformLayer
description: 3D 变换容器图层。用于在图层树中保留 3D 变换效果。
---

`CATransformLayer` 继承自 [CALayer](/reference/quartzcore/calayer/)。它通常作为 3D 变换的容器使用，以便其子图层的 3D 变换能正确合成。

## 实例成员 (Instance members)

当前运行时未额外暴露 `CATransformLayer` 的专属实例成员；可直接使用 `CALayer` 的通用属性与方法来管理图层树与动画。

## 类成员 (Class members)

当前运行时未额外暴露 `CATransformLayer` 的专属类成员。

## 相关

- [CALayer](/reference/quartzcore/calayer/)
